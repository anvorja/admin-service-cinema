# Database (shared Railway PostgreSQL — same as other services)
DATABASE_URL=
DATABASE_URL_USERS=
DATABASE_URL_BOOKING=

# JWT — must match auth-service exactly
JWT_SECRET=
JWT_ALGORITHM=

# Redis (Upstash) — for blacklist checks + catalog cache invalidation
REDIS_URL=

# Cloudinary — for signed upload URLs
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
CLOUDINARY_UPLOAD_PRESET=

# CORS
BACKEND_CORS_ORIGINS=

DEBUG=
